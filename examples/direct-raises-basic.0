fun parse(ok: Bool) -> u8 raises { Invalid } {
  if ok {
    return 7_u8
  }
  raise Invalid
}

fun run() -> u8 raises { Invalid } {
  let value = check parse(true)
  return value
}

export c fun main() -> u8 {
  return run() rescue err { 0_u8 }
}
