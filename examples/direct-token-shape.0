shape Token {
  kind: u8,
  start: usize,
  len: usize = 1,
}

export c fun main() -> u8 {
  let mut token = Token{kind: 65_u8, start: 2}
  token.len = token.start + token.len
  if token.len == 3 {
    return token.kind
  }
  return 0_u8
}
