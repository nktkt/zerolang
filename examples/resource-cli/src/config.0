pub fun defaultName() -> String {
    return "resource-cli.txt"
}

pub fun outputDir() -> String {
    let dir = std.env.get("ZERO_RESOURCE_DIR") rescue missing { ".zero" }
    return dir
}

pub fun outputName() -> String {
    let name = std.args.get(1) rescue missing { defaultName() }
    return name
}
