pub fun main(world: World) -> Void raises {
    let token = Token { kind: 1, offset: 7 }
    if token.kind == 1 && token.offset == 7 {
        check world.out.write("ident\n")
    }
}

shape Token {
    kind: i32,
    offset: i32,
}
