fun add(a: i32, b: i32) -> i32 {
    return a + b
}

fun mix(a: i32, b: i32, c: i32) -> i32 {
    return add(a, b) + c
}

pub fun main(world: World) -> Void raises {
    if mix(10, 20, 12) == 42 {
        check world.out.write("42\n")
    }
}
