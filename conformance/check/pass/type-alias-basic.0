pub type ByteCount = usize
type BytePair = Pair<u8, u8>

shape Pair<T, U> {
  left: T,
  right: U,
}

pub fun main() -> Void {
  let count: ByteCount = 4_usize
  let pair: BytePair = Pair { left: 1_u8, right: 2_u8 }
  expect(count == 4_usize)
  expect(pair.right == 2_u8)
}
