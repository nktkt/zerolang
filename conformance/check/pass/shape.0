shape Point {
    x: i32,
    y: i32,
}

pub fun main(world: World) -> Void raises {
    let point = Point { x: 1, y: 2 }
    if point.x < point.y {
        check world.out.write("shape ok\n")
    }
}
