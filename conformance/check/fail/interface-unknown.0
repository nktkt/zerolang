shape Counter {
  value: i32,
}

fun readValue<T: Missing<T>>(value: ref<T>) -> i32 {
  return 0
}

pub fun main() -> Void {
  let counter: Counter = Counter { value: 1 }
  expect(readValue<Counter>(&counter) == 1)
}
