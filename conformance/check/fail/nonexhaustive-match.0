enum Status {
    ready,
    failed,
}

pub fun main(world: World) -> Void raises {
    let status = Status.ready
    match status {
        ready {
            check world.out.write("ready\n")
        }
    }
}
