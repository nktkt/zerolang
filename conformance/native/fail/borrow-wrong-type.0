shape Point {
    x: i32,
}

shape Other {
    value: i32,
}

fun read_x(point: ref<Point>) -> i32 {
    return point.x
}

pub fun main(world: World) -> Void raises {
    let other = Other { value: 1 }
    let value = read_x(&other)
    check world.out.write("wrong borrow type")
}
