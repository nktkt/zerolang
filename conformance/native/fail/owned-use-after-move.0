shape File {
    fd: i32,
}

fun close(file: owned<File>) -> Void {
}

pub fun main(world: World) -> Void raises {
    let file: owned<File> = File { fd: 1 }
    close(file)
    close(file)
    check world.out.write("use after move")
}
