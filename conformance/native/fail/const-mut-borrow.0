shape Wrapper {
    value: const i32,
}

pub fun main(world: World) -> Void raises {
    let mut wrapper = Wrapper { value: 1 }
    let borrowed = &mut wrapper.value
    check world.out.write("borrowed const")
}
