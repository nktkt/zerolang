shape Point {
    x: i32,
}

pub fun main(world: World) -> Void raises {
    let mut point = Point { x: 1 }
    let shared = &point
    let mutable = &mut point
    check world.out.write("conflicting borrow")
}
