choice Result {
    ok: i32,
    err: String,
}

pub fun main(world: World) -> Void raises {
    let result: Result = Result.ok(42)
    match result {
        ok {
            check world.out.write("native payload match\n")
        }
        err {
            check world.out.write("unexpected\n")
        }
    }
}
