fun add(left: i32, right: i32) -> i32 {
    return left + right
}

pub fun main(world: World) -> Void raises {
    let value: i32 = add(40, 2)
    if value == 42 {
        check world.out.write("params work\n")
    } else {
        check world.out.write("params broke\n")
    }
}
